var Header = React.createClass({
  render: function(){
    return (
      <header>
        <a href="#pending">pending</a>
        <a href="#done">done</a>
        <a href="#all">all</a>
      </header>
    );
  }
});